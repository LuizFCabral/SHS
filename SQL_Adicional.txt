CREATE TABLE vacinacao(
	codigo SERIAL PRIMARY KEY,
	codigo_usuario INT REFERENCES usuario (codigo),
	codigo_agenda INT REFERENCES agenda (codigo),
	codigo_lote INT REFERENCES movimento_vacina (codigo)
);